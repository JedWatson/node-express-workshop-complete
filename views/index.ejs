<!DOCTYPE html>
<html lang="en">
  <head>
    <title>My Blog</title>
    <%- include('includes/htmlhead') %>
  </head>
  <body>
    <h1>Hello world!</h1>
    <% if (posts.length) { %>
      <%= posts.length %> Posts
    <% } else { %>
      No posts yet.
    <% } %>
  </body>
</html>
